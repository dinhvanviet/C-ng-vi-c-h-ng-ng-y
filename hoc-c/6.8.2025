Học và tìm hiểu về giải thuật tìm kiếm:
-Tìm kiếm tuyến tính ,tìm kiếm tuyến tinh và hoán đổi 
-Đếm phần tử.
-Tìm kiếm nhị phân .

demo:tìm kiếm tuyến tính

#include <stdio.h>
int main(){
	int n,x;
	// nhap so nguyen duong n , neu k phai thi nhap lai
	do{
		printf("Nhap so nguyen duong n = ");
		scanf("%d",&n);
		if(n<0){
			printf("Khong phai so nguyen duong ,ban hay nhap lai\n ");
		}
	}while(n<=0);
	// nhap cac phan tu
	int a[n];
	printf("Nhap cac phan tu :\n");
	for(int i=0;i<n;i++){
		scanf(" %d",&a[i]);	
	}
	// nhap so nguyen x
	printf("Nhap x = ");
	scanf("%d",&x);
	
	//so dau tien cua phan tu dau tien co gia tri bang x
	int index = -1;
	printf("Chi so dau tien cua phan tu dau tien co gia tri bang x la :");
	for (int i=0;i<n;i++){
		if(a[i] == x){
			index = i;
			break;
		}
	}
	// in ra phan tu khong ton tai bang -1
	printf("%d",index);
	return 0;
}

 -Đếm phần tử:
 #include<stdio.h>
int main(){
	int n;
	//Nhap so nguyen duong n
	do {
		printf("Nhap n = ");
		scanf("%d",&n);
		if(n<0){
			printf("Khong phai so nguyen duong,hay nhap lai\n");
		}
	}while (n<=0);
	// nhap lan luot cac phan tu
	int a[n];
	for(int i =0;i<n;i++){
		scanf("%d",&a[i]);
	}
	// phan tu nho nhat dau tien cua mang la
	int min=0;
	for(int i=0;i<n;i++){
		if(a[i]<a[min]){
			min=i;
		}
	}
	// phan tu lon nhat cuoi cung cua mang
	int max=n-1;
	for (int i =n-2;i>=0;i--){
		if (a[i]>a[max]){
			max=i;
		}
	}
	// hoan doi phan tu lon nhat dau tien va phan tu lon nhat cuoi cung 
		int temp = a[min];
		a[min] = a[max];
		a[max] = temp;
	
	//in ra sau khi da hoan doi
	for (int i=0;i<n;i++){
		printf("%d",a[i]);
	}
	return 0;
}
